[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "elixi-ai-engine"
version = "1.0.0"
description = "ELIXI AI Engine — FastAPI + Ollama"
requires-python = ">=3.11"
